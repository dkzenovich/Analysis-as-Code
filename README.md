# Analysis as Code: Agentic Framework

**Шаблон репозитория и промпты для системного анализа как кода. Сырые входные данные (транскрипты, заметки) автоматически преобразуются в структурированные спецификации (StRS/SyRS), архитектурные модели и PDR-логи с помощью ИИ-агентов.**

---

## 🚀 Workflow:

⚠️ **Важно:** Весь процесс построен на взаимодействии с AI внутри редактора. Для работы используйте Cursor (переключившись в режим Agent) или VS Code с установленным плагином Codex. Агент имеет доступ к файловой системе, поэтому он сам читает контекст и создает файлы.

### 0. Подготовка (Setup)
Работа ведется локально, чтобы вы могли использовать Git для версионирования и отката изменений.

```bash
# 1. Клонируйте репозиторий-шаблон
git clone https://github.com/dkzenovich/Analysis-as-Code
```

### 1. Этап бизнес-анализа (BA)
*Превращаем неструктурированные «хотелки» в четкие бизнес-требования.*

* **Входные данные (`input/`):**
    Сюда мы загружаем любую сырую информацию, не тратя время на её сортировку:
    * Транскрипты встреч, интервью.
    * Тексты писем и переписок из мессенджеров.
    * Заметки из Notion/Obsidian.

* **Действие:**
    Запускаем промпт: `@prompts/10-stakeholder.md`

* **Результат (`target/docs/src/01_Stakeholder_Needs/`):**
    Агент формирует пакет StRS (Stakeholder Requirements Specification):
    * 📄 `01_stakeholders.md`: Матрица ролей. Кто заказчик, кто пользователь, чьи интересы затронуты и где у них «болит» (Pain Points).
    * 📄 `02_ops_concepts.md`: Концепция операций (OpsCon). Сценарии использования AS-IS (как сейчас) и TO-BE (как надо). Четкая структура: Триггер → Процесс → Бизнес-результат.
    * 📄 `03_constraints.md`: Границы проекта. Бюджеты, сроки, регуляторика. Самое важное здесь — раздел **Out-of-Scope** (что мы *не* делаем).
    * 📄 `04_requirements.md`: Реестр `REQ-BIZ`. Очищенные от технического шума требования, сфокусированные на бизнес-ценности.
    * 📄 `05_open_points.md`: Список выявленных противоречий и вопросов, требующих участия человека.

* **Задача аналитика:**

   * Проверка `05_open_points.md`: Агент собрал вопросы и противоречия. Ваша задача — ответить на них. После этого загрузи вопрос-ответы в новый чат и пороси агента актуализировать файлы в папке `target/docs/src/01_Stakeholder_Needs/`
   * Валидация `04_requirements.md`: Прочитайте требования. Не начал ли AI предлагать технические решения раньше времени? Если да — попросите переписать: "Убери упоминания SQL из требований, оставь только бизнес-суть".
   * Очистка Scope: Проверьте `03_constraints.md`. Убедитесь, что в Out-of-Scope попало именно то, что мы не делаем.

   ⚠️ **Важно:** Перед тем как переходить к следующему этапу или запускать агента повторно, **обязательно** зафиксируйте текущее состояние. Поскольку агент может полностью перезаписать файлы в `target/`, Git — это ваша точка сохранения.

```bash
   # Фиксируем сырые данные и полученные требования
   git add target/docs/src/01_Stakeholder_Needs/
   git commit -m "docs(strs): сформированы бизнес-требования и ограничения"
```

   Теперь, если агент при следующем запуске испортит требования, вы всегда сможете сделать `git restore target/docs/src/01_Stakeholder_Needs/` и вернуть проверенную версию.

---

### 2. Этап анализа влияния (SA Impact)
*Диагностика перед операцией. Проверяем, как новые требования ложатся на старую систему.*

* **Входные данные:**
    * Готовые бизнес-требования (из этапа 1).
    * Папка `reference/` (Текущие API контракты, схемы БД, список интеграций).

* **Действие:**
    Запускаем промпт: `@prompts/20-impact-analysis.md`

* **Результат (`target/docs/src/02_System_Requirements/00_impact_analysis.md`):**
    Критически важный документ, который может остановить разработку. Содержит:
    *  **Executive Summary:** Вердикт (Green/Yellow/Red).
    *  **Impact Matrix:** Таблица, связывающая каждое требование с конкретным файлом компонента (какую таблицу меняем, какой API трогаем).
    *  **Breaking Changes:** Список изменений, ломающих обратную совместимость.
    *  **Migration Strategy:** Нужно ли мигрировать данные и как жить в период Dual-Run.

* **Задача аналитика:**
   *  **Верификация Breaking Changes**: Агент параноик. Если он пометил изменение как "Критичное" (Red), проверьте — действительно ли мы ломаем контракт или это расширение?
   *  **Стратегия миграции:** Агент предложит вариант (например, "Dual Run"). Вы должны подтвердить, что это реально с текущими ресурсами команды.
   *  **Правки:** Если агент не заметил зависимость (например, скрытый вызов в легаси), добавьте это в таблицу руками.
---

### 3. Этап системного проектирования (SA Spec)
*Детальная проработка решения. «Мясо» для разработчиков.*

* **Входные данные:**
    * Результаты Impact-анализа.
    * Бизнес-требования (`REQ-BIZ`).
    * Референсная архитектура.

* **Действие:**
    Запускаем промпт: `@prompts/21-system-spec.md`

* **Результат (`target/docs/src/02_System_Requirements/`):**
    Полный пакет SyRS (System Requirements Specification):
    * 📄 `01_functional.md`: Декомпозиция на системные требования (`REQ-SYS`), жестко связанные с `REQ-BIZ`. Описание Happy Path и Sad Path.
    * 📄 `02_quality.md`: Нефункциональные требования (НФТ). Метрики производительности, безопасности, надежности (SLA/SLO).
    * 📄 `03_interfaces.md`: Спецификация дельты API. Сниппеты OpenAPI/Protobuf, форматы сообщений брокеров. Явные пометки `[NEW]`, `[DEPRECATED]`.
    * 📄 `04_verification.md`: Сценарии приемки в формате **Gherkin** (Given/When/Then). Готовая база для автотестов.

* **Задача аналитика:**
   *  **API Review:** Откройте сгенерированный YAML/JSON в 03_interfaces.md. Правильные ли типы данных? Не забыл ли агент про пагинацию или коды ошибок 4xx/5xx?
   *  **Логика:** Проверьте 01_functional.md. Описаны ли негативные сценарии (Sad Paths)? Если нет — напишите в чат: "Добавь сценарий, когда внешний сервис недоступен по таймауту".
   *  **Тесты:** Гляньте 04_verification.md. Понятны ли эти сценарии QA-инженеру?
---

### 4. Этап принятия решений (PM/Lead)
*Фиксация сложных развилок. Если есть сомнения — пишем ADR.*

* **Входные данные:**
    * Ситуация выбора (например, «Синхронный REST vs Асинхронная очередь»).

* **Действие:**
    Запускаем промпт: `@prompts/30-product-decision.md`

* **Результат (`target/adrs/`):**
    * 📄 **ADR (Architecture Decision Record):** Структурированный документ, описывающий контекст, варианты, критерии сравнения и финальное решение с последствиями.

---

### 5. Этап контроля качества (QA)
*Финальная проверка целостности. Ничто не должно быть потеряно.*

* **Входные данные:**
    * Все сгенерированные спецификации (`target/docs/src/`).

* **Действие:**
    Запускаем промпт: `@prompts/90-traceability-check.md`

* **Результат (`target/docs/src/04_Traceability/`):**
    * 📄 **Traceability Report:** Отчет, подсвечивающий разрывы:
        * Есть ли `REQ-SYS` без родительского бизнес-требования («сироты»)?
        * Все ли требования покрыты критериями приемки?
        * Соответствуют ли изменения в API заявленным требованиям?

* **Задача аналитика:**
   *  **Устранение разрывов:** Если отчет показал "REQ-SYS-12 не связано с REQ-BIZ", вы должны либо привязать требование, либо удалить его как ненужное.
   *  **Финальный OK:** Когда отчет чист, документация считается готовой к передаче в разработку.
---

## 📂 Структура репозитория

```
├── input/                      # 📥 Сырые входящие данные (Read Only)
│   └── README.md
├── reference/                  # 📚 База знаний AS-IS (Source of Truth)
│   ├── db_schema/              # DDL схемы существующих БД
│   ├── openapi/                # Спецификации текущих API
│   └── integrations.md         # Реестр интеграций
├── prompts/                    # 🧠 Инструкции для агентов
│   ├── 00-system-persona.md    # Глобальный контекст и стиль
│   ├── 10-gost-642-stakeholder.md   # BA: Сбор требований (StRS)
│   ├── 20-gost-643-impact-analysis.md # SA: Анализ влияния и рисков
│   ├── 21-gost-643-system-spec.md   # SA: Системные спецификации (SyRS)
│   ├── 30-product-decision.md       # PM: Фиксация решений (ADR/PDR)
│   └── 90-traceability-check.md     # QA: Контроль трассировки
├── target/                     # 🏗 Артефакты работы (генерируются агентами)
│   ├── docs/src/
│   │   ├── 01_Stakeholder_Needs/    # StRS (Бизнес-требования)
│   │   ├── 02_System_Requirements/  # SyRS (Системные требования)
│   │   ├── 03_Logical_Architecture/ # Диаграммы (C4, Sequence)
│   │   └── 04_Traceability/         # Отчеты о трассировке
│   ├── pdrs/                   # 📝 Логи работы агентов (PDR)
│   └── model.dsl               # 🧩 Общая модель C4 (Structurizr)
└── docker-compose.yml          # 🐳 TODO
```


